namespace CStoFlash.AS3Writer {
	using System;

	using DDW;
	using Utils;

	public static class PropertyParser {
		public static void Parse(NamespaceNode pNn, ClassNode pCn, PropertyNode pNode, CodeBuilder pBuilder, ScopeBlock pScope) {
			throw new Exception();
		}
	}
}
