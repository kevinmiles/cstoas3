namespace CStoFlash.AS3Writer {
	using System;

	using DDW;
	using Utils;

	public static class ConstantParser {
		public static void Parse(NamespaceNode pNn, ClassNode pCn, ConstantNode pNode, CodeBuilder pBuilder, ScopeBlock pScope) {
			throw new Exception();
		}
	}
}
