namespace flash.utils {
	/// <summary>
	/// (AIR only)
	/// Defines string constants for the names of compress and uncompress options. These constants are used as values of the algorithm parameter of the <see cref="ByteArray"/>.compress() and <see cref="ByteArray"/>.uncompress() methods. 
	/// </summary>
	public enum CompressionAlgorithm {
		DEFLATE,
		ZLIB
	}
}
